# Assets

Place the onboarding hero image here:

- **onboarding-hero.png** – Used as the background for the top section of the Welcome (step 1) onboarding screen (e.g. the hourglass image).

The image should be in PNG format. Recommended: landscape or square, good resolution (e.g. 800×600 or similar) so it looks sharp on all devices.
